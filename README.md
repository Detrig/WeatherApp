# WeatherApp 🌦

Небольшое pet-project-приложение погоды с упором на:
- **чистый подход (data / domain / presentation)**
- **Jetpack Compose UI**
- **фоновые обновления через WorkManager**
- **уведомления и домашний виджет**

---

## 📸 Скриншоты

| Поиск города | Погода в выбранном городе | Уведомление | Виджет |
| ------------ | ------------------------- | ----------- | ------ |
| ![Поиск города](https://github.com/user-attachments/assets/ba57e92b-62be-40ff-979a-741f8ada10f3) | ![Погода](https://github.com/user-attachments/assets/eb0a2b52-7450-4788-8158-4b2af9498daa) | ![Уведомление о погоде](https://github.com/user-attachments/assets/3f687403-c67b-49d7-a747-dc3af385bc47) | ![Виджет погоды](https://github.com/user-attachments/assets/ec4b02b4-1f88-4fec-a955-97983957a671) |

---
## ✨ Основной функционал

- 🔎 **Поиск города**
  - Поиск по названию через `WeatherAPI` (`/search.json`).
  - Поиск по текущим координатам (Google Play Services Location).
  - Выбор города и сохранение его как “основного”.

- 🌤 **Экран погоды**
  - Текущая погода: температура, ощущаемая, состояние, ветер.
  - Прогноз на несколько дней.
  - Отдельный блок качества воздуха.
  - Кастомный горизонтальный график (Compose `Canvas`) с погодой по часам.

- 💾 **Кэш и оффлайн-режим**
  - Ответ `WeatherAPI` сохраняется в **Room**.
  - При ошибке сети приложение поднимает **последний закешированный прогноз** и продолжает работать.
  - UI сидит на `Flow` из Room → любые обновления кэша автоматически перерисовывают экран.

- 🔁 **Фоновые обновления**
  - `WorkManager` периодически запускает `UpdateWeatherWorker`.
  - Worker вызывает `WeatherRepository.weather()`, обновляет кэш.
  - Изменения автоматом попадают в UI и виджет (через `Flow` / повторный запрос).

- 🔔 **Уведомления о погоде**
  - Настройки уведомлений через `DataStore` (`NotificationsPrefs`), включение/выключение в отдельном экране настроек.

- 🧩 **Виджет (Jetpack Glance)**
  - Домашний виджет с текущей погодой в выбранном городе.
  - Использует `GlanceAppWidget`.
  - Подтягивает данные из того же `WeatherRepository` / Room-кэша.
  - Кликабельный: по нажатию открывает `MainActivity`.

